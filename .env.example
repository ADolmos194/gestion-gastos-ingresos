# Variables que solo usa docker-compose.yml (la raíz) para armar los servicios — Django
# sigue leyendo su propia config de backend/.env (ver env_file en docker-compose.yml).
# Copiá este archivo a ".env" en la raíz del repo.

DB_NAME=gastos_ingresos
DB_USER=gastos_ingresos
DB_PASSWORD=gastos_ingresos_pass

# Puerto del host donde queda nginx (todo el stack se accede por acá: http://localhost).
HTTP_PORT=80

# Origen público completo. Con HTTP_PORT=80 va SIN ":80" (los navegadores omiten el
# puerto por defecto en el header Origin/Referer que Django valida contra
# CSRF_TRUSTED_ORIGINS) — si cambiás HTTP_PORT, agregalo acá también
# (ej: http://localhost:8080) y quedará mal el login/CSRF hasta que coincidan.
PUBLIC_ORIGIN=http://localhost

# Se inlinea en el bundle de React EN BUILD TIME (no se puede cambiar sin reconstruir la
# imagen de nginx). Vacío = pide /api/... al mismo origen que lo sirve (recomendado: sin
# esto, no hace falta configurar CORS en absoluto, todo es same-origin).
VITE_URL_BASE=
